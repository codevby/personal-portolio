<section id="home" class="section hero reveal">
  <div class="container hero-grid">
    <div>
      <p class="eyebrow">Hello, I am</p>
      <h1>Leonardo Garcia</h1>
      <p class="role">Full Stack Developer</p>
      <p class="lead">
        I design and build scalable digital products with a strong focus on software quality, user
        value, and AI-augmented development workflows.
      </p>
      <div class="actions">
        <a href="#contact" class="btn btn-primary">Contact Me</a>
        <a href="#projects" class="btn btn-secondary">View Projects</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero { padding-top: calc(var(--space-8) + var(--space-2)); }
  .hero-grid { display: grid; gap: var(--space-6); }
  .eyebrow { color: var(--secondary); font-weight: 700; margin: 0 0 var(--space-2); }
  h1 { font-size: clamp(2rem, 1.3rem + 4vw, 3.8rem); margin: 0; }
  .role {
    color: var(--primary);
    margin: var(--space-2) 0 var(--space-4);
    font-size: clamp(1.25rem, 1rem + 2vw, 2rem);
    font-weight: 700;
    line-height: 1.3;
  }
  .lead { max-width: 65ch; color: var(--muted); }
  .actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-top: var(--space-5); }
</style>
